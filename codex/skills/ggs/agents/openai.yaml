interface:
  display_name: "Global Git Skills"
  short_description: "Platform-agnostic Git and review workflows"
  default_prompt: "Use $ggs to prepare atomic commits and publish a draft change request for the detected hosting platform."
